import 'package:test/test.dart';

void main() {
  group('Vault handler unit tests', () {
    // Unfortunately, sqflite package does not support flutter_test (yet).
    // All VaultHandler() methods depend on the database.
  });
}
